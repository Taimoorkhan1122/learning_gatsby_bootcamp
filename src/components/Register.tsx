import React from 'react'
import { RouteComponentProps } from '@reach/router'

type Props = RouteComponentProps

const Register: React.FC<Props> = () => {
    return (
        <div>
            <h3>Register page with client side routing</h3>
        </div>
    )
}

export default Register
